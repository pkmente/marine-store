services:
  - type: web
    name: marine-store
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn marine_store.wsgi:application --bind 0.0.0.0:$PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.12
